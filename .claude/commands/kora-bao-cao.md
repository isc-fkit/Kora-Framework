---
description: Báo cáo tiến độ dự án — tự làm mới dữ liệu rồi sinh dashboard (workflows/14)
---

Người dùng vừa CHỦ ĐỘNG gõ `/kora-bao-cao` — đây là **lệnh rõ ràng** để sinh báo cáo tiến độ
(tương đương "báo cáo tiến độ"). KHÔNG hỏi lại "chạy hay chỉ hỏi thông tin".

Đọc và thực thi `workflows/14-progress-report.md` theo `CLAUDE.md`:

- **TỰ làm mới dữ liệu trước:** Cloud `*.atlassian.net` → kéo qua MCP nạp vault; self-host →
  kiểm tra độ mới, nếu CŨ thì báo + nhắc lệnh terminal.
- Sinh dashboard (time-tracking / sprint active / assignee).
- Trả kết quả bằng **UI inline Cowork** + xuất kèm **file HTML**.
