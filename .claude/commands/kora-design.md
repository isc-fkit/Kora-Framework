---
description: Design / build a screen prototype with Claude Design.
---

The user invoked `/kora-design` — an explicit command to build a prototype / open Claude Design.

Read and execute `workflows/04-claude-design.md` following `CLAUDE.md`:

- Gate 2: let the user pick a Design project (existing in `projects/_registry.md`, or create new).
- Generate a design brief from the knowledge in `docs/`, then open / guide Claude Design.
- Keep the Approval Gate.
